import { CloseIcon, FiltersIcon } from '../index';

describe('icons index', () => {
  it('экспортирует CloseIcon', () => {
    expect(CloseIcon).toBeDefined();
  });

  it('экспортирует FiltersIcon', () => {
    expect(FiltersIcon).toBeDefined();
  });
});
